[project]
name = "clingenomics"
version = "0.1.0"
description = "Multi-modal clinical genomics interpretation engine"
requires-python = ">=3.10"
dependencies = ["pydantic>=2.0"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
